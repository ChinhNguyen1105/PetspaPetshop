# Git rule

Assume uncommitted files belong to the user. Inspect status/diff before editing
and preserve unrelated changes. Never use destructive reset/clean/checkout
commands to solve a task. Keep commits (when requested) scoped to one coherent
change and include accompanying documentation/tests.
